# Production Docker Compose Environment Variables
# Copy this file to .env.prod and configure for your deployment

# GitHub Container Registry
GITHUB_USERNAME=your-github-username

# Image tag to deploy (use 'latest', version number, or commit SHA)
IMAGE_TAG=latest

# Application port (exposed on host)
PORT=3000

# Optional: Additional environment variables for the application
# Add any app-specific variables here
